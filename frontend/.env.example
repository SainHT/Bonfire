# Copy to .env.local for local development.
# In production (Cloudflare Pages / Workers), set this in the dashboard.

# Backend base URL. Defaults to http://localhost:8000 if unset.
VITE_API_BASE=http://localhost:8000
